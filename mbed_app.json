{
    "config": {
        "max-redundancy-packets": {
            "help": "The maximum number of redundancy packets this device can process. A higher number requires more heap space.",
            "value": 100
        },
        "ack-mac-commands": {
            "help": "Where to ACK fragmentation / datablock MAC commands",
            "value": 1
        }
    },
    "macros": ["CBC=0", "EBC=1", "MBED_HEAP_STATS_ENABLED=1"],
    "target_overrides": {
        "XDOT_L151CC": {
        }
    }
}
